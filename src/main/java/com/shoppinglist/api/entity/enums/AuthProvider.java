package com.shoppinglist.api.entity.enums;

// This enum will definite the types of authentication providers, such as LOCAL, GOOGLE and FACEBOOK.
public enum AuthProvider {
    LOCAL,
    GOOGLE,
    FACEBOOK
}
